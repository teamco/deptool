<%= form_for(@external_script) do |f| %>
  <% if @external_script.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@external_script.errors.count, "error") %> prohibited this external_script from being saved:</h2>

      <ul>
      <% @external_script.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :script %><br />
    <%= f.text_field :script %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
