<%= form_for(@properties_path) do |f| %>
  <% if @properties_path.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@properties_path.errors.count, "error") %> prohibited this properties_path from being saved:</h2>

      <ul>
      <% @properties_path.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
